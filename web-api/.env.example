# Copy this file to `.env` (which is gitignored) and fill in real values.
# NEVER commit real credentials — only this placeholder example is committed.

# API auth (restful-booker: use its publicly documented demo login)
API_USER=<username>
API_PASSWORD=<password>

# Web login (saucedemo)
WEB_USER=<web-username>
WEB_PASSWORD=<web-password>
